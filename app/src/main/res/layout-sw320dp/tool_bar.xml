<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/toolBarShadow"
    android:orientation="vertical">

    <android.support.v7.widget.Toolbar xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:minHeight="@dimen/toolBarSize"
        android:layout_marginBottom="@dimen/lineSize"
        android:background="@color/toolBarColor"
        app:navigationIcon="@drawable/ic_menu_white_36dp"
        app:titleMargins="@dimen/zero"
        app:titleTextColor="@color/menuTextColor"
        app:titleMarginTop="-4dp"
        app:maxButtonHeight="@dimen/toolBarSize"/>

</LinearLayout>